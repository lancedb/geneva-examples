"""Batch/backfill CLIs: ingest images and videos, chunk videos into clips, seed
fixtures, drop tables, and the feature-engineering stages under ``stages/``.
Each module exposes a Typer ``app`` wired up as a console script.
"""
